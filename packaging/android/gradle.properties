org.gradle.jvmargs=-Xmx4096m
android.useAndroidX=true
# Both ABIs are built by default; see the vcpkgTriplet property on each
# product flavor in app/build.gradle for where this is actually consumed.
android.nonTransitiveRClass=true
